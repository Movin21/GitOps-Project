# Deny all ingress and egress by default within the boutique namespace.
# Explicit allow policies below punch holes for legitimate traffic only.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: boutique
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
